{
  "version": 2,
  "buildCommand": "mkdir -p public && cp app/admin.html public/index.html && cp -r app/assets public/",
  "outputDirectory": "public",
  "installCommand": "echo 'No dependencies needed'",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

