{
  "version": 2,
  "buildCommand": "cd app && yarn install && npx expo export:web",
  "outputDirectory": "app/dist",
  "installCommand": "echo 'Installing in buildCommand'",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

