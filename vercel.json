{
  "version": 2,
  "buildCommand": "yarn --cwd app install && yarn --cwd app expo export:web",
  "outputDirectory": "app/dist",
  "installCommand": "echo 'Skipping root install'",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

